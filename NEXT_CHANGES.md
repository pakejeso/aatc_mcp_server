- [x] **Fix wrong column name in glossary** — The `aact://glossary` resource referenced `outcome_measurements.param_value_string`, but this column does not exist. The actual column is `param_value` (varchar). Fixed in glossary.json.
- [x] **Fix enum value casing in glossary** — Multiple glossary entries used Title Case or old-format values (e.g. `Experimental`, `Parallel Assignment`, `None (Open Label)`) that don't match the actual UPPER_SNAKE_CASE values in the database (e.g. `EXPERIMENTAL`, `PARALLEL`, `NONE`). Fixed: `design_groups.group_type`, `designs.allocation`, `designs.intervention_model`, `designs.masking`, `designs.primary_purpose`, `sponsors.agency_class`.
- [x] **Remove non-existent enum value** — Glossary listed `post_hoc` as a value for `design_outcomes.outcome_type`, but only `primary`, `secondary`, and `other` exist in the data. Removed.
- [x] **Add missing enum values** — `sponsors.agency_class` was missing `UNKNOWN`, `OTHER_GOV`, `INDIV`, `AMBIG`. `designs.primary_purpose` was missing `ECT`. Added.
- [x] **Fix missing `id` columns in schema** — Six tables (`design_groups`, `facilities`, `interventions`, `outcome_analyses`, `outcomes`, `result_groups`) were referenced via `.id` in foreign key definitions but had no `id` column in `aact_schema_static.json`. These are Rails auto-generated serial primary keys that were not captured during the original schema export. Added `id integer NOT NULL PRIMARY KEY` to all six tables.
