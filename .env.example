# AACT Database Connection
# If these are set, the server queries the live PostgreSQL instance.
# If unset or connection fails, it falls back to the bundled static schema.

AACT_DB_HOST=your-rds-instance.amazonaws.com
AACT_DB_PORT=5432
AACT_DB_NAME=aact
AACT_DB_USER=your_username
AACT_DB_PASS=your_password
AACT_DB_SCHEMA=ctgov
