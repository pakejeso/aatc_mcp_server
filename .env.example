# =============================================================================
# AACT MCP Server + Flow Visualizer — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# ---------------------------------------------------------------------------
# Flow Visualizer — LLM Configuration (required for docker-compose)
# The MCP server itself needs no configuration — it reads from the bundled
# static schema file (data/aact_schema_static.json).
# ---------------------------------------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4.1-mini
PORT=8090
